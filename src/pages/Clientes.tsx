import PessoasPage from '../components/PessoasPage'

/** Rota /clientes — pessoas que já realizaram pelo menos uma consulta. */
export default function Clientes() {
  return <PessoasPage mode="clientes" />
}
