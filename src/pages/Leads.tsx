import PessoasPage from '../components/PessoasPage'

/** Rota /leads — pessoas que ainda não compareceram a nenhuma consulta. */
export default function Leads() {
  return <PessoasPage mode="leads" />
}
